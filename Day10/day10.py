file = [[int(c) for c in l] for l in open('input.txt').read()]
height = len(file)
width = len(file[0])

def recursion(li, ci):
    if()

for li,l in enumerate(file):
    for ci, c in enumerate(l):
        if(c == 0):
            sum += recursion(li, ci)